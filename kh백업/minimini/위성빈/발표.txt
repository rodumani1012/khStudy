자바스크립트 라이브러리의 하나

페이스북과 개별 개발자 및 기업들 공동체에 의해 유지보수

npm install create-react-app@2.1.8

create-react-app .

js에서의 함수는 구문이자 값 ( java 에선 method라 불림 : method는 구문임 값이 될 수 없음 )
선언적함수 ( Regular )
익명함수 ( Anonymous )
인라인함수 ( Inline )

=======
내부적인 동작에 있어서 약간의 다른 차이는 있는데,
 일반 함수의 경우는 브라우저 자바 엔진이 javascript 를 코드 전체를 파싱(Parsing)하는 단계에 생성되는 반면,
 인라인 함수는 변수에 대입되는 방식이므로 런타임(Runtime) 시에 생성됩니다.

이렇게 함수가 생성되는 시점으로 본다면 인라인 함수와 익명 함수는 동일합니다.

익명 함수 또한 인라인 함수와 동일하게 런타임 시에 함수가 생성됩니다.

하지만 인라인 함수와 익명 함수는 차이점이 있는데, 이는 재사용성의 여부입니다.

인라인 함수는 특정 변수에 할당 되므로 이 변수를 이용하여 다른 코드에서 함수를 재사용할 수 있습니다.
반면 익명함수는 특정 코드 내에 한정되어 정의되기 때문에 다른 곳에서 재사용할 수 없습니다.
 익명 함수를 할당받은 변수도 없고, 함수명도 없기 때문에 다른 곳에서 접근할 수 있는 방법이 없지요.
=======

( immutable.js (?),  )

npm start

react 구조 설명 : ( component )

function => class, import code 바꾸기

JSX ( JavaScript eXtension ) : 유사 HTML, 그 특징들은 나타낼 때마다 하나 씩

Component 만들기로 하드 코딩해서 기본 페이지 만들자

그리곤 내용들을 props로 넘겨 받아보자

Nav에서의 props는 바로 배열로 넘기는게 아니라 state를 이용하여 넘겨보자

onClick

e.preventDefault

bind

---------------------------------------------------------------------------

실행

시연

1. /react-backend/app.js
2. express-generator
3. /react-backend/models/book.js
4. /react-backend/routes/index.js
5. /react-backend/client/App.js
6. /react-backend/client/components/


 서버와 클라이언트 사이에 중계기로서 대리로 통신을 수행하는 것을 가리켜 '프록시'

https://facebook.github.io/create-react-app/docs/proxying-api-requests-in-development

Access to XMLHttpRequest at 'localhost:3000/api/books/page/undefined'
 from origin 'http://localhost:3000' has been blocked by CORS policy
: Cross origin requests are only supported for protocol schemes
: http, data, chrome, chrome-extension, https.